import {Box, Plane, useHelper} from "@react-three/drei";
import {BoxHelper, PlaneHelper} from "three";
import {useRef} from "react";
// import {PlaneHelper} from "../../../../common/helpers";

export function PlaneHelperComponent({meshRef}){
    // useHelper(meshRef, PlaneHelper)
    const mesh = useRef()
    // useHelper(mesh, PlaneHelper, 2)

    return null
}